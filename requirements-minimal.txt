# Minimal dependencies - chat only, no plugins
# Use when PLUGINS= is empty in .env
# pydub and Pillow needed for transcription/vision (disable via ENABLE_* if unused)
python-dotenv~=1.0.0
pydub~=0.25.1
tiktoken==0.7.0
openai==1.58.1
python-telegram-bot==21.9
requests~=2.32.3
tenacity==8.3.0